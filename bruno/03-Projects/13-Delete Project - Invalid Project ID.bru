meta {
  name: Delete Project - Invalid Project ID
  type: http
  seq: 13
}

delete {
  url: {{base_url}}/v1/projects/invalid-id-abc
  body: none
  auth: none
}

headers {
  Cookie: accessToken={{access_token}}
}

tests {
  test('Status code is 400 or 422', function () {
      expect(res.getStatus()).to.be.oneOf([400, 422]);
  });
}
