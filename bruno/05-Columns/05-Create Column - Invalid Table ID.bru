meta {
  name: Create Column - Invalid Table ID
  type: http
  seq: 5
}

post {
  url: {{base_url}}/v1/cms-columns
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Cookie: accessToken={{access_token}}
}

body:json {
  {
      "tableId": "invalid-uuid",
      "columns": [
          {
              "name": "Column 1"
          }
      ]
  }
}

tests {
  test('Status code is 400 or 404 or 422', function () {
      expect(res.getStatus()).to.be.oneOf([400, 404, 422]);
  });
  
  test('Response has success flag set to false', function () {
      const jsonData = res.getBody();
      expect(jsonData.success).to.equal(false);
  });
}
